#include <stdio.h>

int sum(int a, int b) {
  return a + b;
}
//swap two integers
void swap(int* a, int* b) {
  int temp = *a;

}
// void swap(int a, int b) {
//   int temp = a;
//    a = b;
//    b = temp;
//    printf("%d %d\n", a, b);
// }

int main() {

  int x;
  x = 99;
  int y = 8;
  // double z = 5.3;
  // char c = 'a';

  // // & - address of
  // printf("%p\n", &x);
  // printf("%p\n", &y);
  // int* px = &x; // int* means pointer to int
  // //int * px don't do
  // // int *px don't do

  // //%d-decimal (int)
  // //%f-float
  // //%lf-long float (double)
  // printf("values are %d %d %lf\n", x, y, z);
  // printf("%c\n", c);



   
  //sizeof(int)
  //true=1 false=0



  //printf("Hello World\n");
  //return 0;
}